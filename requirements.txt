# Pinned for C7. Every version here is exercised by the Phase 1 gate and the full
# pipeline run that produces research/reports/.
polars==1.17.1
duckdb==1.1.3
pandera==0.22.1
pyarrow==18.1.0
numpy==2.2.0
scipy==1.14.1
scikit-learn==1.6.0
PyYAML==6.0.1

# Reporting layer (static site generation).
markdown-it-py==3.0.0
mdurl==0.1.2

# Tests.
pytest==8.3.4
