# Pinned for C7. Every version here is exercised by the Phase 1 gate.
polars==1.17.1
duckdb==1.1.3
pandera==0.22.1
pyarrow==18.1.0
numpy==2.2.0
PyYAML==6.0.1

# Reporting layer (static site generation).
markdown-it-py==3.0.0
mdurl==0.1.2

# Tests.
pytest==8.3.4
