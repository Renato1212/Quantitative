__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
.vercel/

# Market data is never committed: immutable, checksummed, local only (spec §4).
# Root-anchored — src/data/ is code and must not be caught by this.
/data/
